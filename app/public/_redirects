# Cloudflare Pages — SPA fallback only.
# WordPress proxy (POST + GET) is handled by app/functions/_middleware.js
# because _redirects 200 rewrites silently drop POST/PUT/DELETE.

/*    /index.html    200
